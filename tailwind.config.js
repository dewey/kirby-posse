/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.vue'],
    // Prefix to avoid conflicts with other Tailwind plugins
    prefix: 'posse-',
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}